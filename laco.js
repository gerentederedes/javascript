for(let contador = 1; contador <= 5; contador++) {
    console.log("Contador é: " + contador);
}       

const palavra = "JavaScript";
for(let i = 0; i < palavra.length; i++) {
    console.log("Letra na posição " + i + " é: " + palavra[i]);
}   
